import { InterpolationConfig } from '../types';

export const DEFAULT_INTERPOLATION_CONFIG: InterpolationConfig = {
  timeRatio: 0.50,
  model: 'RIFE-TIR-v1.0 (Mock)',
  mode: 'High Precision',
};

export const MOCK_GPU_STATUS = 'NVIDIA A100 (Idle)';
